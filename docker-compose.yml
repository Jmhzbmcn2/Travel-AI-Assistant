version: '3.8'

services:
  travel-agent:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DEMO_MODE=true
    restart: unless-stopped
